int32 command_type      # 命令类型：0=ServoMoveStart, 1=ServoMoveEnd; 传输路径默认开始运动，end忽略其他输入
geometry_msgs/PoseArray cartesian_pose # 位姿数组
float64 acc                # 加速度百分比 0-100
float64 vel                # 速度百分比 0-100
float64 cmd_time           # 指令传递周期，单位：秒，推荐范围 [0.001-0.0016]
float64 filter_time        # 滤波时间，单位：秒
float64 gain               # 目标位置比例放大器，默认为0
int32 point_count           # 目标点数量，0=不限制，>0=限制点数
bool use_incremental      # 是否使用增量运动模式，true=增量运动(基坐标系)，false=绝对运动(基坐标系)

---
# 响应部分
bool success               # 操作是否成功
string message             # 返回消息或错误描述
# 备注：cartesian_pose为基坐标系下笛卡尔坐标系下的位姿数组，包含多个点